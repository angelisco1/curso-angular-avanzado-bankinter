import { MiRouterLinkDirective } from './mi-router-link.directive';

describe('MiRouterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MiRouterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
