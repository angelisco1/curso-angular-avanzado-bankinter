import { MiHostDirective } from './mi-host.directive';

describe('MiHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MiHostDirective();
    expect(directive).toBeTruthy();
  });
});
